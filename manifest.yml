---
applications:
- name: respondent-home-ui
  host: respondent-home
  memory: 256M
  instances: 1

env:
  RESPONDENT_HOME_ANALYTICS_ACCOUNT: UA-56892037-7
  RESPONDENT_HOME_EQ_HOST: eq-server
  RESPONDENT_HOME_EQ_PORT: 5000
  RESPONDENT_HOME_IAC_ATTEMPTS_EXPIRATION_SECS: 3600
  RESPONDENT_HOME_IAC_SERVICE_HOST: iacsvc
  RESPONDENT_HOME_IAC_SERVICE_PORT: 80
  RESPONDENT_HOME_LOCALE: en
  RESPONDENT_HOME_MAX_IAC_ATTEMPTS: 5
  # RESPONDENT_HOME_REDIS_* environment variables are taken from the CF VCAP_SERVICES environment variable.

services:
  - respondent-home-ui-redis
